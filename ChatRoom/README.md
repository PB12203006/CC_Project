This is the ChatRoom server code. Server deployed on a ec2 linux instance and messages are transferred using port 8080 on the ec2 instance.  
To run the chatroom server:  
npm install  
node server.js  
